import styled from 'styled-components';

import { FlexCenterAlign } from './../../styling';

export const RegisterWrapper = styled.div`
    ${FlexCenterAlign};

    width: 100vw;
    height: 100vh;
`;
